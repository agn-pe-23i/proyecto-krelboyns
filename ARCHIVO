# menu.py
from catalog import Pelicula, Serie, Documental, EventoDeportivoEnVivo

def mostrar_menu_principal():
    while True:
        print("Menú principal")
        print("1. Agregar un producto")
        print("2. Buscar producto")
        print("3. Eliminar un producto")
        print("4. Mostrar el catálogo")
        print("5. Cargar catálogo")
        opcion = input("Ingrese una opción: ")

        if opcion == "1":
            agregar_producto()
        elif opcion == "2":
            buscar_producto()
        elif opcion == "3":
            eliminar_producto()
        elif opcion == "4":
            mostrar_catalogo()
        elif opcion == "5":
            cargar_catalogo()
        else:
            print("Opción inválida. Por favor, ingrese una opción válida.")

def agregar_producto():
    # Lógica para agregar un producto al catálogo
    pass

def buscar_producto():
    # Lógica para buscar un producto en el catálogo
    pass

def eliminar_producto():
    # Lógica para eliminar un producto del catálogo
    pass

def mostrar_catalogo():
    # Lógica para mostrar el catálogo completo
    pass

def cargar_catalogo():
    # Lógica para cargar el catálogo desde un archivo
   
